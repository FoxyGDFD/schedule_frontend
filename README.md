# Schedule frontend
## Steps to start app:
1. Open project root folder
2. Create `.env` file and fill it or rename `.env.example`
3. Run `npm i && npm run dev`
4. Go to  `localhost:5173` and view app 
